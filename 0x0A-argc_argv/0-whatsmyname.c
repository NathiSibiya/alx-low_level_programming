#include <stdio.h>

/**
 * main - prints the program name and newline
 * @argc: the number arguments supplied to the program
 * @argv: an aaray of pointers to the arguments
 *
 * Return: 0
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
