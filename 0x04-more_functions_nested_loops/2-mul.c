#include "main.h"
/**
 * mul - multiply two integers
 * @a: first integer to be multiplied
 * @b: second integer to be multiplied
 * Return: the results of multiplicaton
 */
int mul(int a, int b)
{
	return (a * b);
}
